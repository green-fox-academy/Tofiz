# - Create a function called `printer`
#   which prints the input parameters
#   (can have multiple number of arguments)

n = (input("Gimme parameters :"))

def printer(text):
        print(text)

printer(n)